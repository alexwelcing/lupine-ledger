{
  "name": "library-lupine-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "Lupine Science research library - mobile-first reader for library.lupine.site",
  "scripts": {
    "build": "node scripts/build.js",
    "content:sync": "node scripts/sync-content-from-science.mjs",
    "content:verify": "node scripts/verify-content.mjs",
    "dev": "node scripts/build.js && node scripts/serve.js",
    "verify": "npm run content:verify && npm run build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "force-graph": "^1.51.4",
    "marked": "^12.0.2"
  }
}
